# Environment / secrets (never commit real credentials)
.env
.env.*
!.env.example

# Local Claude Code settings
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local spec/scratch docs (not part of the repo)
VISITO_1.pdf
